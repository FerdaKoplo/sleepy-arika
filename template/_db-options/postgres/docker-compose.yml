version: "3.8"

services:
  arika-db:
    image: postgres:15-alpine
    container_name: arika-postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: arika-db
